<%= form_with(model: social_work, local: true) do |form| %>
  <% if social_work.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(social_work.errors.count, "error") %> prohibited this social_work from being saved:</h2>

      <ul>
        <% social_work.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :discount %>
    <%= form.number_field :discount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
